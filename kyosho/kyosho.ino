#include "defines.h"
//#define PWM_READ_PIN 13

//The setup function is called once at startup of the sketch
void setup()
{
	int a=PWM_READ_PIN;
	//pwm_reader distance_ref(PWM_READ_PIN);
}

// The loop function is called in an endless loop
void loop()
{
//Add your repeated code here
}
